<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
namespace:必须是对应接口的全路径
-->
<mapper namespace="com.zl.dao.PointDao">
    <insert id="addPoint" parameterType="point">
        insert into SC_POINT (user_id, point_number, point_date, point_type) VALUES (#{userId},#{pointNumber},#{pointDate},#{pointType})
    </insert>
</mapper>
